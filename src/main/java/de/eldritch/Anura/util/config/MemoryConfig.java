package de.eldritch.Anura.util.config;

/**
 * A simple config that is stored in memory.
 */
public class MemoryConfig extends ConfigSection {
    public MemoryConfig() {
        super(null, null);
    }
}
